rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow admin users to list the entire portfolio collection.
    // Also allow them to get, create, update, and delete individual documents.
    match /portfolio/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
