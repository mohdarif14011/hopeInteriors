
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow admin users to read and write to the portfolio collection
    match /portfolio/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
