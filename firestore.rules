
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // A project can be created, updated, or deleted only by an authenticated user.
    // Any visitor to the site can view the projects.
    match /portfolio/{projectId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
